import calculator from './calculator'
import dateGet from './dateGet'
import chart from './chart'

const common = {
    calculator: calculator,
    dateGet: dateGet,
    chart: chart
};

export default common;
